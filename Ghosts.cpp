#include "Ghosts.h"

using namespace std;

Ghosts::Ghosts(){

}

void Ghosts::GetInitPinkyPosition(int& x, int& y)
{
	getCoordinates(x, y);
}

void Ghosts::GetInitDinkyPosition(int& x, int& y)
{
	getCoordinates(x, y);
}